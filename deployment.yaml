apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: springboot
  template:
    metadata:
      labels:
        app: springboot
    spec:
      containers:
      - name: springboot
        image: balaji2307/springboot
        imagePullPolicy: Always
        ports:
           - containerPort : 8080
---
apiVersion: v1
kind: Service
metadata:
  name: springboot
spec:
  type: LoadBalancer
  selector:
    app: springboot
  ports:
  - protocol: "TCP"
    port: 8080
    targetPort: 8080
    
